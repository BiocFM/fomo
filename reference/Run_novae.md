# Run_novae

This function runs novae.

## Usage

``` r
Run_novae(adata_path = NULL, accelerator = "cpu")
```

## Arguments

- adata_path:

  Path to the input anndata file.

- accelerator:

  Accelerator to use, either "cpu" or "cuda". Default is "cpu".
